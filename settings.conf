# Configuration for the DNS server

# File that holds the zone data
zone_file = zone_data.yaml

# Port to listen on
port = 5053

# Upstream DNS server for forwarding
forwarder = 1.1.1.1

# Enable query logging (true/false)
query_logging = true

# Query log file path
query_log_file = query.log
